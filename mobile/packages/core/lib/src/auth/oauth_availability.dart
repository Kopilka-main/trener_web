/// Показывать ли кнопки соц-входа (VK/Яндекс) в приложении.
///
/// Включены на всех платформах, включая iOS (по решению владельца).
/// ⚠️ Apple Guideline 4.8: при наличии стороннего соц-логина требуется ещё и
/// «Вход через Apple». Пока его нет — есть риск отклонения обновления на ревью.
/// Чтобы снова скрыть на iOS: вернуть `import 'dart:io';` и `=> !Platform.isIOS;`.
/// Единственное место, от которого зависят все экраны входа/регистрации.
bool get oauthLoginEnabled => true;
